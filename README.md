# SmartFloodAI: LSTM-Based Flood Forecasting & Alert System



**SmartFloodAI** เป็นระบบคาดการณ์ระดับน้ำและแจ้งเตือนน้ำท่วมล่วงหน้า พัฒนาขึ้นเพื่อช่วยให้ชุมชนให้สามารถเตรียมพร้อมรับมือกับอุทกภัยโดยใช้โมเดล Deep Learning (LSTM) ในการวิเคราะห์ข้อมูลระดับน้ำจาก Web Service API ของรัฐบาล เพื่อคาดการณ์แนวโน้มระดับน้ำในอนาคต และแจ้งเตือนประชาชนผ่าน Line Chatbot

---

## 🏛️ System Architecture

ระบบถูกออกแบบมาให้ทำงานอัตโนมัติและต่อเนื่อง โดยมีส่วนประกอบหลักดังนี้:

*

[Image of System Architecture Diagram]
*

1.  **Data Ingestion:** Google Cloud Scheduler สั่งรันสคริปต์ Python เพื่อดึงข้อมูลระดับน้ำล่าสุดจาก Web Service API ของรัฐบาล
2.  **Data Processing:** ข้อมูลจะถูกทำความสะอาด (Data Cleaning) และจัดเก็บลงในฐานข้อมูล MySQL
3.  **Predictive Modeling:** สคริปต์อีกตัวจะดึงข้อมูลที่สะสมไว้มาประมวลผลผ่านโมเดล LSTM (Scikit-Learn/TensorFlow) เพื่อคาดการณ์ระดับน้ำล่วงหน้า 3 ชั่วโมง
4.  **Data Storage:** ผลการคาดการณ์จะถูกบันทึกกลับไปที่ MySQL
5.  **Web Dashboard:** React Frontend ดึงข้อมูลจากฐานข้อมูลมาแสดงผลเป็นกราฟแนวโน้มและระดับน้ำปัจจุบัน
6.  **Alert System:** หากผลการคาดการณ์มีระดับเกินเกณฑ์ที่กำหนด ระบบจะยิงข้อความแจ้งเตือนไปยังประชาชนผ่าน Line Chatbot

---

## ✨ Key Features

* **LSTM-Based Forecasting:** คาดการณ์ระดับน้ำล่วงหน้าด้วยโมเดล Time-Series ที่มีความแม่นยำ
* **Real-Time Dashboard:** Web UI (React + Tailwind) แสดงผลข้อมูลระดับน้ำปัจจุบันและกราฟคาดการณ์
* **Automated Alerts:** ระบบแจ้งเตือนอัตโนมัติผ่าน Line Chatbot เมื่อระดับน้ำถึงจุดเฝ้าระวัง
* **Automated Workflow:** ใช้ Google Cloud Scheduler ในการรันระบบทั้งหมด
* **Secure Connection:** เชื่อมต่อกับฐานข้อมูล on-premise (เช่น ของโรงเรียน) อย่างปลอดภัยผ่าน HTTP Tunneling และ Reverse Proxy โดยไม่ต้องเปิด Public Port

---

## 🛠️ Technology Stack

* **Backend & ML:** Python, TensorFlow (LSTM), Pandas, NumPy
* **Frontend:** React, Tailwind CSS
* **Database:** MySQL
* **DB Management:** phpMyAdmin
* **DevOps & Services:** Google Cloud Scheduler, Line Messaging API, AppServ (on-prem), Secure Reverse Proxy (HTTP Tunneling)
* **Libraries:** `requests`, `scikit-learn`, `numpy`

---

## ⚙️ Setup & Installation

1.  **Clone the repository:**
    ```bash
    git clone [https://github.com/your-username/FloodSense-Predictive-Water-Alert-System.git](https://github.com/your-username/FloodSense-Predictive-Water-Alert-System.git)
    cd FloodSense-Predictive-Water-Alert-System
    ```

2.  **Backend Setup (Python):**
    ```bash
    cd backend
    pip install -r requirements.txt
    ```

3.  **Frontend Setup (React):**
    ```bash
    cd ../frontend
    npm install
    ```

4.  **Database Setup:**
    * Import `database/schema.sql` ไปยัง MySQL Server ของคุณ
    * ตั้งค่าการเชื่อมต่อในไฟล์ `.env`

5.  **Environment Variables:**
    สร้างไฟล์ `.env` ในโฟลเดอร์ `backend` โดยอ้างอิงจาก `.env.example`:
    ```ini
    MYSQL_HOST=your_host
    MYSQL_USER=your_user
    MYSQL_PASSWORD=your_password
    MYSQL_DB=flood_db
    GOVERNMENT_API_KEY=xxx
    LINE_CHANNEL_ACCESS_TOKEN=yyy
    LINE_CHANNEL_SECRET=zzz
    ```

6.  **Run the application:**
    * **Run Frontend:** `npm start` (ในโฟลเดอร์ `frontend`)
    * **Run Backend Scripts:** ตั้งค่า Cron หรือ Google Cloud Scheduler ให้รันสคริปต์ใน `backend/scripts/` ตามลำดับ

---

## 🚀 Future Improvements

* [ ] เพิ่มแหล่งข้อมูล (เช่น สภาพอากาศ, ปริมาณน้ำฝน) เพื่อเพิ่มความแม่นยำ
* [ ] พัฒนา LLM Chatbot ให้สามารถตอบคำถามเกี่ยวกับสถานการณ์น้ำได้
* [ ] ปรับปรุงโมเดล LSTM ให้ซับซ้อนยิ่งขึ้น (เช่น Stacked LSTM, Bi-LSTM)

---

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.
